# Prompt Sections For topic-intake

This skill comes from:

- `<ROLE>`
- `<TASK>`
- `<INPUT_DATA>`
- `<INPUT_FIELD_DEFINITIONS>`

Key inherited rules:

- The agent creates official community topics for U.S. stock investors.
- The package includes background image prompt, title, summary, poll, English keywords, and U.S. stock names and tickers.
- `TOPIC_THEME` defines the main event but cannot support unsupported facts.
- `SOURCE_LINKS` are for attribution and traceability, not facts by themselves.
- `SOURCE_CONTENT` is the main factual material.
- `SPECIAL_REQUIREMENTS` can guide emphasis but cannot override factuality.

